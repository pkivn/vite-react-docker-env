# Vite React Docker Development Environment Variables

# Node environment
NODE_ENV=development

# Vite server port
PORT=5173

# Host configuration (0.0.0.0 allows connections from outside the container)
VITE_HOST=0.0.0.0

# Enable HMR for development
VITE_HMR=true

# Enable polling for file changes (useful in Docker environments)
CHOKIDAR_USEPOLLING=true

# API URL if needed
# VITE_API_URL=http://localhost:3000/api

# Other Vite-specific configuration
VITE_APP_TITLE=Vite React App
VITE_DEV_SERVER_PORT=5173
